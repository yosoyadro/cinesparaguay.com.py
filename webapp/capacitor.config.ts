import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'py.com.cinesparaguay',
  appName: 'Cines Paraguay',
  webDir: 'dist',
  bundledWebRuntime: false,
  backgroundColor: '#581c87'
};

export default config;
